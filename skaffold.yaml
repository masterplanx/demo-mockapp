apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/masterplanx/demo-eks:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/masterplanx/demo-eks:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: demo-mockapp
        chartPath: charts/demo-mockapp
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/masterplanx/demo-eks"
          image.tag: "{{.DIGEST_HEX}}"
